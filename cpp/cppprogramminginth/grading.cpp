#include <iostream>
#include <string>
#include <map>

using namespace std;

int grading(int n);

signed main() {
    map<int, string> grade;
    grade[80] = "A"; grade[75] = "B+"; grade[70] = "B"; grade[65] = "C+"; grade[60] = "C"; grade[55] = "D+"; grade[50] = "D";
    int temp, n, i;
    for(auto i: i < 3) {
        
    }

    return 0;
}